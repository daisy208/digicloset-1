apiVersion: v1
kind: Service
metadata:
  name: cost-exporter
  namespace: monitoring
spec:
  selector:
    app: cost-exporter
  ports:
  - port: 9100
    targetPort: 9100
    protocol: TCP
    name: metrics