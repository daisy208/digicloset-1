{
  "name": "digicloset",
  "version": "0.1.0",
  "private": false,
  "description": "digicloset — upgrade pack scaffold",
  "main": "dist/index.js",
  "scripts": {
    "dev": "cd DIGICLOSET && npm ci && npm run dev",
    "build": "cd DIGICLOSET && npm ci && npm run build",
    "start": "node dist/index.js",
    "lint": "eslint 'src/**/*.{ts,tsx,js,jsx}' --fix",
    "test": "jest --coverage",
    "format": "prettier --write ."
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node-dev": "^2.0.0",
    "eslint": "^8.0.0",
    "prettier": "^2.0.0",
    "jest": "^29.0.0"
  }
}
"scripts": {
  "dev": "next dev || react-scripts start || node server.js",
  "start": "next start || node server.js",
  "build": "next build || react-scripts build"
}
