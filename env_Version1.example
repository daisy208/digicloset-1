# Copy to .env and set secure values
# Secret used to sign JWTs. Use a long random value in production.
JWT_SECRET=change-this-to-a-secure-random-value

# Token lifetime (e.g. '1h', '7d')
JWT_EXPIRES_IN=1h

# If you use the SSH deploy in CI/CD, those secrets remain the same:
# SSH_HOST=...
# SSH_USER=...
# SSH_KEY=...
# SSH_PORT=22